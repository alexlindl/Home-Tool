configuration:
  db_password:
    name: Database Password
    description: Password for the PostgreSQL database user. Change this from the default for security.
  db_name:
    name: Database Name
    description: Name of the PostgreSQL database. Only change if you know what you're doing.
  db_user:
    name: Database User
    description: Username for the PostgreSQL database connection.
